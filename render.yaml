# Render Blueprint for DAML Agent Tokenization Platform
# Deploy to Render using external PostgreSQL database

services:
  # Canton Blockchain Service (Docker)
  - type: web
    name: canton-agent-tokenization
    env: docker
    region: oregon
    plan: starter
    dockerfilePath: ./Dockerfile

    # Environment Variables
    envVars:
      - key: DATABASE_URL
        value: postgresql://canton_user:x9VjIV327PRzVQF3kkckk0EuyYNqbCbV@dpg-d32rup8dl3ps738cr23g-a/canton_agent_tokenization
      - key: PORT
        value: 10000

    # Health check
    healthCheckPath: /v1/packages

    # Auto-deploy on Git push
    autoDeploy: false